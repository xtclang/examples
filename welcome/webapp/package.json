{
  "name": "welcome",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.1"
  },
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"No frontend tests: the Create React App boilerplate test was removed in the move to Vite.\" && exit 1",
    "eject": "echo \"Nothing to eject: the Vite build configuration is already in vite.config.js.\""
  }
}
